[project]
name = "fly-in"
version = "0.1.0"
description = "An efficient drone routing and simulation system."
requires-python = ">=3.12"
dependencies = [
    # "tuiloom==0.3.0",
	"tuiloom @ file:///home/maroard/Bureau/42/Tuiloom",
    "pydantic==2.13.5",
]

[dependency-groups]
dev = [
    "flake8>=7.0",
    "mypy>=1.11",
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
